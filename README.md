# personal-finance

**Tally**, a local app that reads bank and card e-statements and answers where the money went,
how much the home cost, and what is worth changing. The code is in [`app/`](app/README.md).

Only code is in this repository. Statements, the database, settings and reports stay on the
computer that runs Tally; `.gitignore` keeps them out.
